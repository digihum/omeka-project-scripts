This repository contains template bash scripts that are used in the build process for omeka projects.

`build-and-deploy.sh` is fired from within the target environment. This does need some rethinking.
